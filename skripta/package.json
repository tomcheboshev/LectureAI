{
  "name": "lectureai",
  "version": "1.0.0",
  "private": true,
  "description": "Root dev orchestration — runs the Express API and the Vite client together so the frontend never starts without its backend (the cause of Vite's proxy ECONNREFUSED errors on /api/*).",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
